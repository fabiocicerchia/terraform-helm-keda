# Backstage catalog entry, read by paved's scorecard plugin.
#
# Every check paved scores is derived from this file alone — no cluster
# access, no CI credentials. What is absent here is absent from the score.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: terraform-helm-keda
  description: >-
      Terraform module to deploy KEDA (Kubernetes Event Driven Autoscaling) on Kubernetes using Helm.
  annotations:
    github.com/project-slug: fabiocicerchia/terraform-helm-keda
    backstage.io/source-location: url:https://github.com/fabiocicerchia/terraform-helm-keda/
  links:
    - url: https://github.com/fabiocicerchia/terraform-helm-keda
      title: Source
spec:
  type: library
  lifecycle: production
  owner: group:default/kodebeat
  system: oss-tools
